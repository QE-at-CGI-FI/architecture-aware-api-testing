meta {
  name: PATCH node - no auth - 401
  type: http
  seq: 3
}

patch {
  url: http://localhost:8000/nodes/spelling-errors
  body: json
  auth: none
}

body:json {
  {
    "description": "Updated"
  }
}

tests {
  test("status is 401", function() {
    expect(res.status).to.equal(401);
  });
}
