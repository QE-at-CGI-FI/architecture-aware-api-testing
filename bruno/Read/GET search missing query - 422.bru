meta {
  name: GET search missing query - 422
  type: http
  seq: 18
}

get {
  url: http://localhost:8000/search
  body: none
  auth: none
}

tests {
  test("status is 422", function() {
    expect(res.status).to.equal(422);
  });
}
